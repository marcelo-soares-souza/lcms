<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to "Todos", users_path, :class => "nav-link active" %>
  </li>
</ul>

<div class="tab-content">
  <div class="tab-pane active"><br/>
    <table class="table table-striped">
      <thead>
        <tr>
          <th></th>
          <th>Name</th>
          <th>E-Mail</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= link_to (image_tag Gravatar.new(user.email).image_url), user_path(user) %></td>
          <td><%= link_to user.fullname, user_path(user) %></td>
          <td><%= user.email %></td>
          <td>
            <!-- <%= link_to 'Editar', edit_user_path(user), :class => 'btn btn-success btn-sm' %> -->
            <%= link_to 'Apagar', user_path(user), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-danger btn-sm' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
